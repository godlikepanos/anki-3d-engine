language: cpp

compiler:
  - gcc

addons:
  apt:
    packages:
      - gcc-4.8
      - g++-4.8
      - cmake

env:
  - CC=gcc-4.8
  - CXX=g++4.8

script:
  - cd build
  - cmake .. -DANKI_BUILD_TOOLS=OFF
  - make -j 2

branches:
  only:
    - master

notifications:
  email: false

os:
  - linux


